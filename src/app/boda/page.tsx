import About from "@/components/bodaview/About";

export default function Boda() {
    return (
        <main className="h-full w-full flex flex-col items-center justify-between bg-white">
            <About/>
        </main>
    );
}